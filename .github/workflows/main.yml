name: GitHub-Profile-Summary-Cards

on:
  schedule: # execute every 24 hours
    - cron: "* */24 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate

    steps:
      - uses: actions/checkout@v2
      - uses: mdabir1203/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{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}}
        with:
          USERNAME: ${{github.mdabir1203}}
